---
import VocabularyPage from "@components/pages/VocabularyPage.astro";
import Layout from "@layouts/Layout.astro";
import pb from "@lib/db";
import type { Translation } from "@types";
const translations:Translation[] = await pb.collection("Translation").getFullList();

export async function getStaticPaths() {
    return [];
}
---

<Layout>
    <VocabularyPage translations={translations}/>
</Layout>