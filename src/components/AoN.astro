---
import AonLogo from "@assets/NethysMask.svg";
interface Props {
  url: string;
}

const { url } = Astro.props;
---

<a
  href={`https://2e.aonprd.com${url}`}
  target="_blank"
  rel="noopener noreferrer"
  class="btn btn-primary"><AonLogo width={24} height={24} /></a
>
